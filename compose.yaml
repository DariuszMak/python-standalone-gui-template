version: '3.8'

services:
  app:
    build:
      context: .
      args:
        PYTHON_VERSION: 3.11
    volumes:
      - .:/app
      - ./linux_distribution:/app/linux_distribution
    working_dir: /app
    entrypoint: ""
